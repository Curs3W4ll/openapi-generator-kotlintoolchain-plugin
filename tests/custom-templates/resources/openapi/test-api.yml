---
openapi: 3.0.3
info:
  title: Custom Templates Test API
  version: 0.0.1
  description: Minimal spec used to exercise the templateDir override

components:
  schemas:
    PostDto:
      type: object
      required:
        - id
      properties:
        id:
          type: integer
          format: int64

paths:
  /posts/{id}:
    get:
      operationId: getPost
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: integer
            format: int64
      responses:
        '200':
          description: The post matching the id
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/PostDto'
