---
components:
  schemas:
    PostDto:
      type: object
      required:
        - id
        - status
      properties:
        id:
          type: integer
          format: int64
        status:
          type: string
          enum:
            - active
            - archived
