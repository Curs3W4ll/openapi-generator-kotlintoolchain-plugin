---
components:
  schemas:
    UserDto:
      type: object
      required:
        - id
        - role
      properties:
        id:
          type: integer
          format: int64
        role:
          type: string
          enum:
            - admin
            - member
